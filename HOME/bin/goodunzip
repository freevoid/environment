#!/usr/bin/env python
from entrypoint import entrypoint
import zipfile

@entrypoint
def main(filename, path=None, password=None):
    zf = zipfile.ZipFile(filename)
    zf.extractall(path=path, pwd=password)

